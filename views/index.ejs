<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Create user</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h2 class="text-3xl font-semibold tracking-tight mb-4">Create Account</h2>
      <div class="w-1/4 mx-auto flex items-center justify-center mt-10">
        <form
          action="/register"
          method="post"
          class="flex flex-col gap-2 w-full"
        >
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
            type="text"
            placeholder="name"
            name="name"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
            type="text"
            placeholder="username"
            name="username"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
            type="password"
            placeholder="password"
            name="password"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
            type="email"
            placeholder="email"
            name="email"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none"
            type="number"
            placeholder="age"
            name="age"
          />
          <input
            class="px-3 py-2 rounded-md bg-blue-500 text-semibold cursor-pointer"
            type="submit"
            value="Create Account"
          />
        </form>
      </div>
      <p class="w-1/4 mx-auto mt-2">
        Already a User,
        <a class="text-blue-500 font-semibold" href="/login">login</a>
      </p>
    </div>
  </body>
</html>
